void f47(char & s, int g)
{
	char * test;
	
	outp(s);
}

void f49(char str, int k)
{
	outp(str);
}

void f50()
{	
	int number;  // Comment
	char ch; int x; int i; 
	int * p;
	char ss[] = "hello"; //
	int tmp[10];
	
	//	while(number != /*test*/ 0)
	//	{
	number = number + 25;
	number = 5-7;
	f49(45);
	tmp[1] = -5 / 9 + 8 / 2 + tmp[5+number];
//	ch = 5 * 9 + 8 / 2 * f49(45, 25);
	
	outp(152);
	outp("test");
	number--;
	//	int i;

	
	//	}
	
	inp(ch);
}


int main() 
{
	int number;  // Comment
	char ch; int x; int i;
	char ss[] = "hello"; //
	int tmp[10];

	outp(ss);

	f50();


	
	number = 15; 
	tmp[5] = 4;
	
	wh(number != /*test*/ 0)
	{		
		number = 5-7+4;
		outp("test");
		
		number--;
	//	int i;
		
		
	}
	
	inp(ch);
}