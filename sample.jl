void f47(char s, int g)
{
	char * test;
	
	outp(s);
}

void f49(int num)
{
	outp(num);
}

void f50()
{	
	int number;  // Comment
	char ch; int x; int i; 
	int * p;
	char ss[] = "hello"; //
	int tmp[10];
	
	outp(ss);
	//	while(number != /*test*/ 0)
	//	{
	number = 25;
	outp(number);

	number = number;
	outp(number);

	number = number + 25;
	outp(number);
	number = 7 - 5;
	outp(number);
	f49(525);
	tmp[1] = -5 / 9 + 8 / 2 + tmp[5+number];
//	ch = 5 * 9 + 8 / 2 * f49(45, 25);
	
	outp(152);
	outp("test");
	number--;
	//	int i;

	
	//	}
	
	inp(ss);
	outp(ss);
}


int main() 
{
	int number;  // Comment
	char ch; int x; int i;
	char ss[] = "Hi!"; //
	int tmp[10];

	outp(22);
	outp("Hello World!");
	outp(ss);

	f50();


	
	number = 15; 
	outp(number);

	//outp(tmp{5]);

	number = 2;
	
	wh(number != /*test*/ 0)
	{		
		outp("test");
		outp(number);
		number = number - 1;
	//	int i;
		
		
	}
	
	inp(ss);
	outp(ss);

}
